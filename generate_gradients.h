#ifndef GENERATE_GRADIENTS_H_INCLUDED
#define GENERATE_GRADIENTS_H_INCLUDED
#include <vector>

using namespace std;

// Declaring the function responsible for creating a vector for the gradients.
vector<float> generate_gradients(int data_size, const vector<float>& filtered);


#endif // GENERATE_GRADIENTS_H_INCLUDED
